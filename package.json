{
  "name": "@flatina/flget",
  "version": "0.1.1",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "bun build ./src/cli.ts --outdir ./dist --entry-naming flget.js --target bun --sourcemap=external --minify",
    "start": "bun run ./src/cli.ts",
    "test": "bun test",
    "test:e2e": "bun test ./tests/e2e",
    "test:deployed-all": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/test-deployed-all.ps1"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "^1.3.3",
    "typescript": "^5.9.2"
  }
}
